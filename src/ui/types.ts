export enum AppView {
  Menu = 'menu',
  SelectProvider = 'select-provider',
  InputPath = 'input-path',
  Loading = 'loading',
  Browser = 'browser',
  Stats = 'stats',
  Preview = 'preview',
  Exporting = 'exporting',
  Complete = 'complete',
  TaggingSetup = 'tagging-setup',
  Settings = 'settings'
}

export enum AppMode {
  Export = 'export',
  Browse = 'browse',
  Stats = 'stats'
}

export enum MenuOption {
  Source = 'source',
  Browse = 'browse',
  Stats = 'stats',
  Tagging = 'tagging',
  Settings = 'settings',
  Exit = 'exit'
}
